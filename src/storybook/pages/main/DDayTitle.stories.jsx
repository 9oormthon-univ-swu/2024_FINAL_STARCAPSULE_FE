import DDayTitle from '@/pages/main/DDayTitle';

const meta = {
    title: 'main/DDayTitle',
    component: DDayTitle,
    tags: ['autodocs'],
};

// 기본 스토리 설정
const Default = {
    args: {},
};

export default meta;
export { Default };
