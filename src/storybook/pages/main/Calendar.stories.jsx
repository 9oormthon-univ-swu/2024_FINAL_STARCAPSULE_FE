import Calendar from '@/pages/main/Calendar/Calendar';

const meta = {
    title: 'main/Calendar',
    component: Calendar,
};

export default meta;

const Default = () => <Calendar />;

export { Default };
