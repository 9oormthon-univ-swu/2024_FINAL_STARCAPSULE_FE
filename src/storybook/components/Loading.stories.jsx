import Loading from '@/components/Loading';

const meta = {
    title: 'components/Loading',
    component: Loading,
    tags: ['autodocs'],
};

const Default = {
    args: {},
};

export default meta;
export { Default };
